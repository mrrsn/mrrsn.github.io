let totalSeconds=5,expectedShots=3,shotThreshold=30,debounceMs=100,calibrationOffsetMs=0,beepOnShot=!1;export function setTotalSeconds(e){totalSeconds=Number(e)}export function getTotalSeconds(){return totalSeconds}export function setExpectedShots(e){expectedShots=Number(e)}export function getExpectedShots(){return expectedShots}export function setTotalSecondsSafe(e){const t=Number(e);!Number.isFinite(t)||t<1||(totalSeconds=Math.floor(t))}export function setExpectedShotsSafe(e){const t=Number(e);!Number.isFinite(t)||t<0||(expectedShots=Math.max(0,Math.floor(t)))}export function setThreshold(e){const t=Number(e);Number.isFinite(t)&&(shotThreshold=Math.min(127,Math.max(0,t)))}export function getThreshold(){return shotThreshold}export function setDebounceMs(e){const t=Number(e);!Number.isFinite(t)||t<0||(debounceMs=Math.round(t))}export function getDebounceMs(){return debounceMs}export function setCalibrationOffset(e){const t=Number(e);Number.isFinite(t)&&(calibrationOffsetMs=Math.round(t))}export function getCalibrationOffset(){return calibrationOffsetMs}export function setBeepOnShot(e){beepOnShot=Boolean(e)}export function getBeepOnShot(){return beepOnShot}